jQuery(document).on("click", 'a[href^="#"],a[href^="' + window.location.origin + window.location.pathname + '#"]', (function(e) {
    e.preventDefault();
    var a = jQuery(this).attr("href"),
        n = a.indexOf("#"),
        t = a.substring(n);
    if (1 !== t.length) {
        var r = jQuery(t);
        if ("#main" === t && (r = jQuery("main")), 0 !== r.length) {
            var i = 1.2 * jQuery("#site-header").outerHeight(),
                o = r.offset().top - i;
            "main" !== r.prop("nodeName").toLowerCase() ? (jQuery("body").hasClass("admin-bar") && (o -= 32), jQuery(r).hasClass("ui-tabs-panel") && (o -= 50), jQuery("body, html").stop().animate({
                scrollTop: o
            })) : r.focus()
        }
    }
})), jQuery(document).ready((function() {
    jQuery(document).on("click", ".video", (function(e) {
        e.preventDefault;
        var a = jQuery(this);
        let n = jQuery(this).find("video").get(0);
        1 == n.paused ? (n.play(), a.addClass("playing")) : (n.pause(), a.removeClass("playing"))
    }))
}));