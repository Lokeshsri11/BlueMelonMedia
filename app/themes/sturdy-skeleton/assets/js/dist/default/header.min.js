jQuery(document).ready((function() {
    let e, a = 0,
        n = jQuery(".header-placeholder"),
        r = jQuery(".site-header").height();
    jQuery(window).scroll((function(a) {
        e = !0
    })), setInterval((function() {
        e && (! function() {
            var e = jQuery(this).scrollTop();
            if (Math.abs(a - e) <= 5) return;
            e > r ? (n.height(r), e > a && e > r ? (jQuery("header").removeClass("nav-down").addClass("nav-up"), jQuery("body").addClass("header-spacing")) : e + $(window).height() < $(document).height() && jQuery("header").removeClass("nav-up").addClass("nav-down")) : (n.height("auto"), jQuery("body").removeClass("header-spacing"), jQuery("header").removeClass(["nav-down", "nav-up"]));
            a = e
        }(), e = !1)
    }), 0);
    let s = jQuery(".language-switcher");
    s.length && s.on("click", (function() {
        let e = jQuery(this);
        e.find(".lang-dropdown");
        e.hasClass("open") ? e.removeClass("open") : e.addClass("open")
    }))
}));